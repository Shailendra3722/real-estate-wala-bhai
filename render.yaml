services:
  - type: web
    name: real-estate-api
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node simple-api-server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        sync: false
